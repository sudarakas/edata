PUBLIC_HOST=http://localhost
PORT=8080
# IMPORTANT: Update these database credentials for your environment.
# Do not use default credentials in production.
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=edata_db

# IMPORTANT: You must set a strong and unique secret for JWT signing. Do not use weak or default values.
JWT_SECRET=""
